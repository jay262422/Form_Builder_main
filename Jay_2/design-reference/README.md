# Builder layout design references (static)

Visual-only HTML snapshots saved when **Builder Layout** app routes were removed (June 2026). The product uses **Form Builder** (`VisualFormBuilder`) as the primary editor; these files are design references only.

- `studio-layout.html` — Studio builder concept (3 columns: sections + library | canvas | inspector)
- `docs-layout.html` — Docs / Word-style concept (ribbon + outline | page canvas | side panel)

Open in a browser. No JavaScript required; not wired to the product.
